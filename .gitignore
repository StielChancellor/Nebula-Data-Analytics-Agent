# --- OS / editor ---
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
*~

# --- Node ---
node_modules/
.pnpm-store/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm
.eslintcache

# --- Vite / frontend builds ---
frontend/**/dist/
frontend/**/dist-*/
frontend/**/.vite/
frontend/**/build/
frontend/**/coverage/
frontend/**/.turbo/

# --- Env files (never commit secrets) ---
.env
.env.local
.env.*.local
frontend/.env
frontend/.env.local
frontend/.env.*.local
backend/.env
backend/.env.local

# Brand env files MAY be committed if they only contain non-secret config (e.g. VITE_API_BASE).
# If they contain anything secret, add explicit ignores here.

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# venv
.venv/
venv/
env/
ENV/

# mypy / pytest / coverage
.mypy_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.ruff_cache/

# --- Terraform ---
infra/terraform/.terraform/
# .terraform.lock.hcl IS committed per Terraform's recommendation — pins
# provider versions so the next clone gets the same plan output.
# State (incl. timestamped backups like terraform.tfstate.1700000000.backup)
# must NEVER be committed — it can contain sensitive values.
**/*.tfstate
**/*.tfstate.*
infra/terraform/terraform.tfvars
infra/terraform/*.tfplan
infra/terraform/crash.log

# --- GCP / firebase / cloud build ---
*.gcp-key.json
service-account-*.json
.firebase/
.firebaserc.local

# --- Generated / cache ---
generated/
.cache/
*.tsbuildinfo

# --- Misc ---
*.log
.DS_Store
