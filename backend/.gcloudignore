# Keep Cloud Build source uploads small + clean.
# The Dockerfile only COPYs pyproject.toml, packages/, services/ — everything
# else is noise (and .venv is ~hundreds of MB).
.venv/
**/__pycache__/
**/*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
.env
.env.*
